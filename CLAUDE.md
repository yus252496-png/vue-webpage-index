# AGENTS.md

本文件是 Codex 在 `d:\ClawWork\vue-app` 项目内工作的项目级规则。除非用户明确覆盖，所有自动化修改、排查、测试、提交说明都应遵守这里的约定。

## 项目概况

- 项目类型：Vue 3 单页应用（SPA），智能管理系统，赛博朋克风格。
- 项目名称：`小余老师项目 - 智能管理系统`
- 技术栈：Vue 3（Options API + Composition API 混用）、Vite 5、Vue Router 5、Vuex 4、Element Plus、Axios。
- 构建工具：Vite 5，开发服务器默认端口 `5173`。
- 包管理：npm（存在 `package.json` 和 `package-lock.json`）。
- 主要入口：`index.html` → `src/main.js` → `src/App.vue`。
- 路由模式：`createWebHistory()`（非 hash 模式），通过 `vercel.json` 的 rewrite 规则处理 SPA fallback。
- 部署平台：Vercel。
- Git 远端：`git@github.com:yus252496-png/vue-webpage-index.git`，分支 `main`。
- 当前没有 TypeScript、ESLint、Prettier 或测试框架配置。
- 数据流：Vuex 管理全局状态（认证、主题），localStorage 持久化 token 和主题偏好，provide/inject 用于布局层级跨组件通信（如全屏状态）。

## 沟通语言

- 日常沟通必须使用中文，直奔主题，优先说明结论、改动和验证结果。
- 技术报错、异常名、API 名称、命令输出、库名、配置键、HTTP 状态码等保留英文原文。
- 如果出现错误，不要只翻译或概括；必须保留关键英文错误信息，方便用户复制到搜索引擎查询。
- 解释技术方案时可以中文为主，英文术语保留原名，例如 `Vuex`、`Vue Router`、`Element Plus`、`localStorage`、`provide/inject`、`scoped`。
- 用户英文基础较弱，避免要求用户用英文补充需求；需要确认时用中文问一个关键问题即可。

## 工作方式与用户偏好

- 默认以效率和结果为导向，不频繁确认，不把简单事项拆成多轮询问。
- 需求足够明确时直接执行；只有在继续操作会带来明显风险或产生不可逆影响时才先询问。
- 中间反馈要短，说明正在做什么、发现了什么、下一步是什么。
- 不要为了显得顺利而掩盖问题。无法完成、验证失败、环境缺失、网络失败都要真实说明。
- 不要编造不存在的测试、构建结果、Git 远端、依赖版本或浏览器验证。

## 编码与文件规范

- 所有文本文件使用 `UTF-8` 保存，尤其是 `.vue`、`.js`、`.css` 文件和中文内容。
- 本项目使用 Vue 3 SFC（单文件组件），遵循 `.vue` 文件结构：`<template>` → `<script>` → `<style scoped>`。
- 组件命名使用 PascalCase（例如 `MainLayout.vue`、`StyleSwitcher.vue`），视图文件放在 `src/views/`，可复用组件放在 `src/components/`。
- 变量和函数命名应表达业务含义，例如 `renderChart`、`updateMetrics`、`fetchStockData`，避免 `a`、`data1`、`temp2` 这类含义不明的命名。
- 可同时使用 Options API 和 Composition API（`<script setup>`），两者在本项目中并存，不强制统一。新增组件优先考虑 `<script setup>` 写法。
- 逻辑应按职责分组：数据定义、生命周期、方法、计算属性、侦听器。
- CSS 变量集中放在 `:root`（位于 `cyberpunk-theme.css` 中定义的各主题变量）；颜色、边框、阴影、间距尽量复用已有 CSS 变量。
- 组件样式使用 `<style scoped>` 避免污染全局；需要覆盖 Element Plus 默认样式时，使用全局样式文件（`global-styles.css` / `cyberpunk-theme.css`）。
- 不要删除现有视觉效果或交互，除非需求明确要求替换。
- 不引入 TypeScript、不引入新的 UI 库或状态管理方案，除非用户明确要求。

## 前端与视觉规则

- 本项目是赛博朋克风格的管理系统，视觉改动应保持沉浸式大屏、科技感、数据动态感。
- 默认主题为 `cyberpunk`（霓虹青/品红暗色），支持 6 套主题（`cyberpunk`、`minimal`、`classic`、`modern`、`comic`、`sketch`），通过 Vuex 管理并在 `<html>` / `<body>` 上切换 class。
- 页面应在桌面大屏上优先表现良好，同时不能在常见移动宽度（<768px）下出现明显文字重叠或布局崩坏。
- 使用 `clamp()`、CSS Grid、Flex、`minmax()` 等方式保证响应式布局稳定。移动端侧边栏使用汉堡菜单折叠。
- 不要让文本溢出按钮、卡片、标题栏或指标区域；必要时使用换行、截断或缩小容器内字号。
- 动态数据应看起来实时变化，但不要制造过高频率导致页面卡顿。
- 使用 Element Plus 组件时，注意主题兼容性；切换主题后应检查关键组件显示是否正常。

## 安全与红线

- 禁止在代码中硬编码个人密码、账号凭据、API Key、Access Token、Refresh Token、Cookie、私钥或真实业务密钥。
- 当前 `src/config/api.js` 中硬编码了 DeepSeek API key，这是已知安全隐患。后续应优先迁移到环境变量（`.env` 文件），不得新增硬编码密钥。
- 禁止提交 `.env`、`.env.local`、`.env.*`、密钥文件、导出的浏览器 Cookie、真实用户数据或本地机器敏感路径。
- 如果需要示例配置，只能提交 `.env.example`，并使用占位符，例如 `YOUR_API_KEY_HERE`。
- 不要把用户个人信息、真实订单、真实客户名单或内部业务数据写入演示页面。需要数据时使用模拟数据。
- 引入外部 CDN 或第三方资源前，要考虑可用性和隐私影响；不要加入未知来源脚本。
- 不执行破坏性命令，例如 `git reset --hard`、批量删除、清空目录，除非用户明确要求并确认目标路径。

## 测试与验证

- 开发前优先思考可验证行为；能写自动检查时先补检查，再改实现。
- 当前项目没有测试框架，因此每次修改后至少做与改动匹配的真实验证：
  - **语法检查**：运行 `npm run build`（或 `npx vite build`），确认构建无报错。
  - **开发服务器检查**：运行 `npm run dev`（或 `npx vite`），检查终端是否有模块解析错误。
  - **控制台验证**：打开页面，检查浏览器控制台是否有 `ReferenceError`、`TypeError`、`SyntaxError` 或模块加载失败。
  - **路由验证**：涉及路由改动时，检查导航守卫、懒加载路径是否正确，认证路由跳转是否符合预期。
  - **视觉验证**：检查桌面和窄屏宽度下是否有重叠、溢出、空白区域、不可读文字。
  - **主题验证**：涉及样式改动时，至少在 `cyberpunk`（默认）和 `minimal`（浅色）两个主题下检查效果。
  - **动态验证**：涉及 `setInterval`、定时器、WebSocket 或模拟数据刷新时，观察至少一个刷新周期。
- 如果本地环境无法运行浏览器验证，要明确说明"未完成浏览器验证"，并给出已完成的替代检查。
- 报告验证结果时必须真实：写出执行过的命令、检查过的页面、是否通过，以及失败的英文错误原文。
- 不允许使用"应该可以""大概率没问题"代替验证结果。

## 常用命令

```bash
# 安装依赖
npm install

# 启动开发服务器（端口 5173，自动打开浏览器）
npm run dev

# 构建生产版本（输出到 dist/）
npm run build

# 预览构建结果
npm run preview
```

## Git 与提交规范

- 提交前必须先检查工作区状态，区分自己修改和用户已有修改，不要回滚用户未授权的改动。
- 提交信息使用简洁中文，格式建议：

  ```
  <type>: <简短描述>

  - 具体改动 1
  - 具体改动 2（可选）
  ```

常见类型：

- `feat`: 新增页面、功能模块或交互
- `fix`: 修复样式、脚本错误、路由问题、主题兼容性或数据展示问题
- `style`: 调整视觉样式，不改变行为
- `refactor`: 代码结构整理，不改变外部行为
- `chore`: 依赖更新、构建配置、部署配置等工程性变更
- `docs`: 文档变更

- 推送远端前必须确认目标分支和远端名称；当前远端为 `origin`，默认分支为 `main`。
- 不要把验证失败的代码提交为"完成"。如果用户要求保留阶段性提交，提交信息中要说明仍有未解决问题。

## CodeGraph

本项目的上层提示可能要求优先使用 CodeGraph 做结构化代码查询。

- 如果需要使用 CodeGraph 且工具返回 `CodeGraph not initialized`，应询问用户是否运行 `codegraph init -i`。
- 在 CodeGraph 可用时，用它回答符号、调用关系、影响范围等结构问题。
- 对静态文本、页面文案、错误字符串、CSS 颜色值等字面搜索，使用 `rg` 或 Grep 工具。

## 修改流程

1. 先阅读相关文件和需求，不凭印象改。
2. 找到最小改动范围，保持现有项目风格和结构。
3. 修改前说明即将改哪些文件。
4. 修改后进行真实验证（构建检查、控制台检查、视觉检查）。
5. 最终回复用中文说明：改了什么、验证了什么、是否还有风险。

## 不应做的事

- 不把 Vue 3 项目迁移到其他框架（React、Angular 等）。
- 不引入 TypeScript，除非用户明确要求。
- 不引入额外的构建工具或打包器，Vite 已足够。
- 不引入真实业务密钥、真实用户数据或不可公开素材。
- 不在未验证时声称"已修复""已完成""测试通过"。
- 不覆盖用户已有改动，不擅自格式化无关文件。
- 不把中文需求改写成只有英文的文档或界面。
- 不强制统一 Options API 和 Composition API 的写法风格，两者在本项目中并存是可接受的。
